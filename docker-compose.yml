version: '3.7'

services:
    app:
        image: 880403025195.dkr.ecr.us-east-2.amazonaws.com/portfolio-app:latest
        volumes:
            - static_volume:/opt/venv/lib/python3.10/site-packages/portfolio;
        expose:
          - 5678
    nginx:
        image: 880403025195.dkr.ecr.us-east-2.amazonaws.com/portfolio-nginx:latest
        restart: unless-stopped
        volumes:
            - static_volume:/opt/venv/lib/python3.10/site-packages/portfolio;
            - nginx_secrets:/etc/letsencrypt
        ports:
            - 80:80
            - 443:443
        depends_on:
            - app

volumes:
    static_volume:
    nginx_secrets:
